# 앱인토스 로그인 라우트 + 세션 발급 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 앱인토스 SPA가 보낸 authorizationCode를 받아 토스 토큰교환→사용자조회→신원매핑→Supabase 세션 발급까지 처리하는 백엔드 로그인 라우트를 완성한다.

**Architecture:** 기존 Next(Vercel) API 라우트에 `POST /api/apps-in-toss/login` 추가. 순수 로직은 주입형으로 TDD, 실제 배선(mTLS fetch·서비스 클라이언트)은 라우트에서 조립. 세션은 generateLink(magiclink)→verifyOtp로 서버에서 직접 발급(메일 미발송). live 실행은 mTLS 인증서·client_id/secret·API 호스트(R6~R8) 수령 후.

**Tech Stack:** Next.js route handler, @supabase/supabase-js, undici(mTLS dispatcher), vitest.

**전제(이미 존재):** `lib/apps-in-toss/tossLogin.ts`(exchangeTossCode/fetchTossUser), `lib/apps-in-toss/identity.ts`(resolveUserByTossKey/shadowEmail), `lib/supabase/service.ts`(createServiceClient).

---

## 파일 구조
- `lib/apps-in-toss/tossSession.ts` (Create) — `mintSupabaseSession()` generateLink→verifyOtp
- `lib/apps-in-toss/tossSession.test.ts` (Create)
- `lib/apps-in-toss/loginFlow.ts` (Create) — `handleTossLogin()` 주입형 오케스트레이터
- `lib/apps-in-toss/loginFlow.test.ts` (Create)
- `lib/apps-in-toss/mtls.ts` (Create) — mTLS fetch 어댑터(런타임, 유닛테스트 없음)
- `app/api/apps-in-toss/login/route.ts` (Create) — 얇은 라우트(조립+호출)

---

## Task 1: mintSupabaseSession (TDD)

**Files:** Create `lib/apps-in-toss/tossSession.ts`, `lib/apps-in-toss/tossSession.test.ts`

- [ ] **Step 1: 실패 테스트** — `lib/apps-in-toss/tossSession.test.ts`:
```ts
import { describe, it, expect } from "vitest"
import { mintSupabaseSession } from "./tossSession"

function fakeAdmin(linkResult: unknown) {
  return { auth: { admin: { generateLink: async () => linkResult } } } as never
}
function fakeVerifier(otpResult: unknown) {
  return { auth: { verifyOtp: async () => otpResult } } as never
}

describe("mintSupabaseSession", () => {
  it("generateLink→verifyOtp로 세션 토큰을 발급한다", async () => {
    const admin = fakeAdmin({ data: { properties: { hashed_token: "ht_1" } }, error: null })
    const verifier = fakeVerifier({ data: { session: { access_token: "at", refresh_token: "rt" } }, error: null })
    const r = await mintSupabaseSession(admin, verifier, "toss_x@apps.takca.internal")
    expect(r).toEqual({ accessToken: "at", refreshToken: "rt" })
  })
  it("매직링크 토큰이 없으면 에러", async () => {
    const admin = fakeAdmin({ data: { properties: {} }, error: null })
    const verifier = fakeVerifier({ data: { session: null }, error: null })
    await expect(mintSupabaseSession(admin, verifier, "e")).rejects.toThrow("매직링크 생성 실패")
  })
  it("세션 발급 실패 시 에러", async () => {
    const admin = fakeAdmin({ data: { properties: { hashed_token: "ht_1" } }, error: null })
    const verifier = fakeVerifier({ data: { session: null }, error: { message: "bad otp" } })
    await expect(mintSupabaseSession(admin, verifier, "e")).rejects.toThrow("세션 발급 실패: bad otp")
  })
})
```

- [ ] **Step 2: 실행→실패 확인** — `npm test` (module 없음)

- [ ] **Step 3: 구현** — `lib/apps-in-toss/tossSession.ts`:
```ts
import type { SupabaseClient } from "@supabase/supabase-js"

export interface MintedSession {
  accessToken: string
  refreshToken: string
}

/**
 * shadow 유저(비번 없음)에게 서버에서 Supabase 세션을 직접 발급한다.
 * admin: service-role 클라이언트(generateLink). verifier: 세션 미저장 클라이언트(verifyOtp).
 * 메일은 발송되지 않는다(admin generateLink는 토큰만 반환).
 */
export async function mintSupabaseSession(
  admin: SupabaseClient,
  verifier: SupabaseClient,
  email: string
): Promise<MintedSession> {
  const { data: link, error: linkErr } = await admin.auth.admin.generateLink({
    type: "magiclink",
    email,
  })
  const hashedToken = link?.properties?.hashed_token
  if (linkErr || !hashedToken) {
    throw new Error(`매직링크 생성 실패: ${linkErr?.message ?? "no token"}`)
  }
  const { data: otp, error: otpErr } = await verifier.auth.verifyOtp({
    token_hash: hashedToken,
    type: "email",
  })
  if (otpErr || !otp?.session) {
    throw new Error(`세션 발급 실패: ${otpErr?.message ?? "no session"}`)
  }
  return {
    accessToken: otp.session.access_token,
    refreshToken: otp.session.refresh_token,
  }
}
```

- [ ] **Step 4: 통과 확인** — `npm test`
- [ ] **Step 5: 타입체크** — `npx tsc --noEmit` → exit 0
- [ ] **Step 6: Commit**
```bash
git add lib/apps-in-toss/tossSession.ts lib/apps-in-toss/tossSession.test.ts
git commit -m "feat: shadow 유저 Supabase 세션 발급(mintSupabaseSession, generateLink→verifyOtp)"
```

---

## Task 2: handleTossLogin 오케스트레이터 (TDD)

**Files:** Create `lib/apps-in-toss/loginFlow.ts`, `lib/apps-in-toss/loginFlow.test.ts`

- [ ] **Step 1: 실패 테스트** — `lib/apps-in-toss/loginFlow.test.ts`:
```ts
import { describe, it, expect } from "vitest"
import { handleTossLogin } from "./loginFlow"

function deps(overrides: Partial<Parameters<typeof handleTossLogin>[0]> = {}) {
  return {
    exchange: async () => ({ accessToken: "at", refreshToken: "rt", expiresIn: 3600 }),
    fetchUser: async () => ({ userKey: "uk_1", name: "홍길동", phone: "010" }),
    resolve: async () => ({ userId: "user-1", isNew: true }),
    mint: async () => ({ accessToken: "sb_at", refreshToken: "sb_rt" }),
    ...overrides,
  }
}

describe("handleTossLogin", () => {
  it("교환→조회→매핑→세션까지 순서대로 처리하고 세션을 반환한다", async () => {
    const r = await handleTossLogin(deps(), { authorizationCode: "c", referrer: "DEFAULT" })
    expect(r).toEqual({ accessToken: "sb_at", refreshToken: "sb_rt", userId: "user-1", isNew: true })
  })
  it("role 미지정 시 shipper 기본, userKey를 resolve에 넘긴다", async () => {
    let seenKey = ""; let seenRole = ""
    const r = await handleTossLogin(deps({
      resolve: async (userKey, profile) => { seenKey = userKey; seenRole = profile.role; return { userId: "u", isNew: false } },
    }), { authorizationCode: "c", referrer: "DEFAULT" })
    expect(seenKey).toBe("uk_1")
    expect(seenRole).toBe("shipper")
    expect(r.isNew).toBe(false)
  })
  it("교환 실패는 위로 전파된다", async () => {
    await expect(handleTossLogin(deps({
      exchange: async () => { throw new Error("토큰 발급 실패: 401") },
    }), { authorizationCode: "c", referrer: "DEFAULT" })).rejects.toThrow("토큰 발급 실패: 401")
  })
})
```

- [ ] **Step 2: 실행→실패 확인** — `npm test`

- [ ] **Step 3: 구현** — `lib/apps-in-toss/loginFlow.ts`:
```ts
import type { MintedSession } from "./tossSession"
import type { TossTokenResponse, TossUser } from "./tossLogin"

export interface TossLoginDeps {
  exchange: (code: string, referrer: string) => Promise<TossTokenResponse>
  fetchUser: (accessToken: string) => Promise<TossUser>
  resolve: (
    userKey: string,
    profile: { name: string; role: "shipper" | "driver" }
  ) => Promise<{ userId: string; isNew: boolean }>
  mint: (userKey: string) => Promise<MintedSession>
}

export interface TossLoginBody {
  authorizationCode: string
  referrer: string
  role?: "shipper" | "driver"
}

export interface TossLoginResult {
  accessToken: string
  refreshToken: string
  userId: string
  isNew: boolean
}

/**
 * 앱인토스 로그인 오케스트레이션(순수, 주입형).
 * 교환→사용자조회→신원매핑→Supabase 세션 발급.
 */
export async function handleTossLogin(
  deps: TossLoginDeps,
  body: TossLoginBody
): Promise<TossLoginResult> {
  const token = await deps.exchange(body.authorizationCode, body.referrer)
  const user = await deps.fetchUser(token.accessToken)
  const resolved = await deps.resolve(user.userKey, {
    name: user.name ?? "토스사용자",
    role: body.role ?? "shipper",
  })
  const session = await deps.mint(user.userKey)
  return {
    accessToken: session.accessToken,
    refreshToken: session.refreshToken,
    userId: resolved.userId,
    isNew: resolved.isNew,
  }
}
```

- [ ] **Step 4: 통과** — `npm test`
- [ ] **Step 5: 타입체크** — `npx tsc --noEmit` → 0
- [ ] **Step 6: Commit**
```bash
git add lib/apps-in-toss/loginFlow.ts lib/apps-in-toss/loginFlow.test.ts
git commit -m "feat: 앱인토스 로그인 오케스트레이터 handleTossLogin(주입형·TDD)"
```

---

## Task 3: mTLS 어댑터 + 로그인 라우트 (배선, 빌드검증)

이 태스크는 런타임 배선이라 유닛테스트 없이 `tsc`/build로 검증한다. live 실행은 R6~R8(인증서·credential·호스트) 수령 후.

**Files:** Create `lib/apps-in-toss/mtls.ts`, `app/api/apps-in-toss/login/route.ts`

- [ ] **Step 1: undici 설치 확인** — Run: `node -e "require.resolve('undici'); console.log('ok')"`. 있으면 그대로, 없으면 `npm i undici`.

- [ ] **Step 2: mTLS 어댑터** — `lib/apps-in-toss/mtls.ts`:
```ts
import { Agent, fetch as undiciFetch } from "undici"

/**
 * 파트너 API용 mTLS 클라이언트 인증서를 붙인 fetch를 만든다.
 * cert/key는 PEM 문자열(환경변수 주입). Vercel 서버리스 아웃바운드 mTLS 검증 필요(R6).
 */
export function createMtlsFetch(cert: string, key: string): typeof fetch {
  const dispatcher = new Agent({ connect: { cert, key } })
  return ((input: Parameters<typeof fetch>[0], init?: Parameters<typeof fetch>[1]) =>
    undiciFetch(input as never, { ...(init as never), dispatcher } as never)) as unknown as typeof fetch
}
```

- [ ] **Step 3: 로그인 라우트** — `app/api/apps-in-toss/login/route.ts`:
```ts
import { NextResponse } from "next/server"
import type { NextRequest } from "next/server"
import { createClient } from "@supabase/supabase-js"
import { createServiceClient } from "@/lib/supabase/service"
import { exchangeTossCode, fetchTossUser } from "@/lib/apps-in-toss/tossLogin"
import { resolveUserByTossKey, shadowEmail } from "@/lib/apps-in-toss/identity"
import { mintSupabaseSession } from "@/lib/apps-in-toss/tossSession"
import { handleTossLogin } from "@/lib/apps-in-toss/loginFlow"
import { createMtlsFetch } from "@/lib/apps-in-toss/mtls"

export async function POST(request: NextRequest) {
  const env = {
    apiBase: process.env.TOSS_PARTNER_API_BASE,
    cert: process.env.TOSS_MTLS_CERT,
    key: process.env.TOSS_MTLS_KEY,
    supabaseUrl: process.env.NEXT_PUBLIC_SUPABASE_URL,
    anonKey: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
  }
  if (!env.apiBase || !env.cert || !env.key || !env.supabaseUrl || !env.anonKey) {
    return NextResponse.json({ error: "server not configured" }, { status: 503 })
  }

  let body: { authorizationCode?: string; referrer?: string; role?: "shipper" | "driver" }
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: "invalid body" }, { status: 400 })
  }
  if (!body.authorizationCode || !body.referrer) {
    return NextResponse.json({ error: "authorizationCode/referrer 필요" }, { status: 400 })
  }

  const mtlsFetch = createMtlsFetch(env.cert, env.key)
  const cfg = { apiBase: env.apiBase }
  const service = createServiceClient()
  // 세션 미저장 클라이언트(verifyOtp 전용)
  const verifier = createClient(env.supabaseUrl, env.anonKey, {
    auth: { persistSession: false, autoRefreshToken: false },
  })

  try {
    const result = await handleTossLogin(
      {
        exchange: (code, ref) => exchangeTossCode(mtlsFetch, cfg, code, ref),
        fetchUser: (at) => fetchTossUser(mtlsFetch, cfg, at),
        resolve: (userKey, profile) => resolveUserByTossKey(service, userKey, profile),
        mint: (userKey) => mintSupabaseSession(service, verifier, shadowEmail(userKey)),
      },
      { authorizationCode: body.authorizationCode, referrer: body.referrer, role: body.role }
    )
    return NextResponse.json(result)
  } catch (e) {
    return NextResponse.json(
      { error: e instanceof Error ? e.message : "login failed" },
      { status: 502 }
    )
  }
}
```

- [ ] **Step 4: 타입체크 + 빌드** — Run: `npx tsc --noEmit` (exit 0), 그다음 `npm run build` (성공, `/api/apps-in-toss/login` 라우트 컴파일 확인). `npm test`도 여전히 통과.

- [ ] **Step 5: Commit**
```bash
git add lib/apps-in-toss/mtls.ts app/api/apps-in-toss/login/route.ts package.json package-lock.json
git commit -m "feat: 앱인토스 로그인 라우트 + mTLS fetch 어댑터(배선, live는 R6~R8 대기)"
```

---

## 후속(범위 밖)
- **live 검증**: TOSS_PARTNER_API_BASE(R7)·TOSS_MTLS_CERT/KEY(R6)·client 인증(콘솔) Vercel 환경변수 설정 후 샌드박스 appLogin end-to-end
- SPA: 로그인 응답의 access/refresh로 `supabase.auth.setSession` + 역할 온보딩 화면
- 핵심 플로우 화면(#4), 에스크로 결제(#5)

## 자체 검토
- **커버리지:** #3 잔여(세션발급·오케스트레이션·라우트·mTLS) 담당. 순수로직 2개 TDD, 배선 1개 빌드검증.
- **자리표시자:** 없음. env 미설정 시 503 반환은 실동작(placeholder 아님). mTLS live 검증만 후속.
- **타입 일관성:** `handleTossLogin` deps(exchange/fetchUser/resolve/mint) 시그니처가 기존 `exchangeTossCode`/`fetchTossUser`/`resolveUserByTossKey`/`mintSupabaseSession`와 라우트에서 어댑터로 정확히 연결. `MintedSession`·`TossTokenResponse`·`TossUser` 재사용.
