# 앱인토스 수익화 A안 (기사 매칭 이용료) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 매칭 성사 시 기사에게 건당 정액(3,000원) 매칭 이용료를 토스페이로 과금하고, 결제해야 상대 연락처·채팅이 열리며, 24시간 미결제 시 매칭이 자동취소되는 수익화 기능을 구현한다.

**Architecture:** 순수 비즈니스 로직은 주입형(DI) 오케스트레이터로 분리해 vitest로 TDD하고(토스페이 HTTP·Supabase는 주입), Next.js 라우트가 실제 의존성을 배선한다. 프론트(Vite SPA)의 MatchDetail이 `match_fees.status`로 게이팅한다. 토스페이 실결제는 mTLS 인증서·가맹점키·앱 승인 후 연결하며, 그 전까지 로직·화면·테스트는 완성된다.

**Tech Stack:** Next.js App Router(백엔드 라우트), Supabase(service-role/RLS), Vite+React 19(미니앱 SPA), vitest, `@apps-in-toss/web-framework`(SDK `checkoutPayment`), 토스페이 파트너 API(`apps-in-toss-api.toss.im`, mTLS).

**선행 블로커:** 마이그레이션 018(orders↔matches RLS 재귀 수정)이 프로덕션에 적용돼 있어야 매칭·조회가 동작한다. 미적용이면 본 기능 전체가 동작 불가.

---

## 파일 구조

- Create `supabase/migrations/019_match_fees.sql` — match_fees 테이블 + RLS
- Create `lib/apps-in-toss/matchFeeConfig.ts` — MATCH_FEE_AMOUNT 상수, 토스 API 경로 상수
- Create `lib/apps-in-toss/tossPay.ts` — 토스페이 make-payment/execute-payment 순수 호출(DI fetch)
- Create `lib/apps-in-toss/tossPay.test.ts`
- Create `lib/apps-in-toss/matchFeeFlow.ts` — payMatchFee/confirmMatchFee/expireMatchFee 오케스트레이터(주입형)
- Create `lib/apps-in-toss/matchFeeFlow.test.ts`
- Modify `lib/apps-in-toss/approveBid.ts` — 매칭 생성 직후 fee 생성 dep 추가
- Modify `lib/apps-in-toss/approveBid.test.ts`
- Modify `app/api/apps-in-toss/bids/approve/route.ts` — insertMatchFee 배선
- Create `app/api/apps-in-toss/match-fee/create/route.ts` — payToken 발급
- Create `app/api/apps-in-toss/match-fee/confirm/route.ts` — 결제 승인 → paid
- Create `app/api/apps-in-toss/match-fee/expire/route.ts` — 만료 매칭 정리
- Modify `apps-in-toss/src/screens/MatchDetail.tsx` — fee 게이팅 + 결제 CTA
- Modify `apps-in-toss/src/screens/More.tsx` — 매칭 이용료·환불 약관 조항
- Modify `vercel.json` — 만료 스윕 크론(백스톱)

---

### Task 1: 마이그레이션 019 (match_fees 테이블 + RLS)

**Files:**
- Create: `supabase/migrations/019_match_fees.sql`

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- ============================================================
-- 019: 매칭 이용료(기사 과금) 테이블
-- ============================================================
CREATE TABLE IF NOT EXISTS match_fees (
  id                uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  match_id          uuid NOT NULL REFERENCES matches(id) ON DELETE CASCADE,
  driver_id         uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  amount            integer NOT NULL CHECK (amount >= 0),
  status            text NOT NULL DEFAULT 'pending'
                    CHECK (status IN ('pending','paid','refunded','cancelled')),
  toss_payment_key  text,
  toss_order_no     text,
  paid_at           timestamptz,
  expires_at        timestamptz NOT NULL,
  created_at        timestamptz NOT NULL DEFAULT now(),
  UNIQUE(match_id)
);

CREATE INDEX IF NOT EXISTS idx_match_fees_match_id ON match_fees(match_id);
CREATE INDEX IF NOT EXISTS idx_match_fees_status_expires ON match_fees(status, expires_at);

ALTER TABLE match_fees ENABLE ROW LEVEL SECURITY;

-- 조회: 해당 기사, 또는 매칭 화주(018 SECURITY DEFINER 헬퍼 재사용 → 재귀 회피)
CREATE POLICY "match_fees_select" ON match_fees FOR SELECT TO authenticated
  USING (
    driver_id = auth.uid()
    OR EXISTS (
      SELECT 1 FROM matches m
      WHERE m.id = match_id AND public.is_shipper_of_order(m.order_id, auth.uid())
    )
  );
-- insert/update는 서버(service-role)만 → 유저 정책 없음(기본 거부)
```

- [ ] **Step 2: 로컬 검증(문법)**

Run: `psql -f supabase/migrations/019_match_fees.sql` 가능 환경이 없으면 스킵. 대신 SQL을 눈으로 검토: `is_shipper_of_order`가 018에 정의돼 있는지 확인.
Run: `grep -n "is_shipper_of_order" supabase/migrations/018_fix_orders_matches_rls_recursion.sql`
Expected: 정의 라인 출력됨.

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/019_match_fees.sql
git commit -m "feat: 019 match_fees 테이블 + RLS(018 헬퍼 재사용)"
```

> ⚠️ 프로덕션 적용: 018 적용 후 대시보드 SQL Editor에서 019 실행. 019는 018의 `is_shipper_of_order`에 의존.

---

### Task 2: 상수 정의 (matchFeeConfig)

**Files:**
- Create: `lib/apps-in-toss/matchFeeConfig.ts`

- [ ] **Step 1: 상수 파일 작성**

```ts
// 매칭 이용료 정책 상수. 금액 변경은 여기서만.
export const MATCH_FEE_AMOUNT = 3000
export const MATCH_FEE_TTL_MS = 24 * 60 * 60 * 1000 // 24시간

// 토스페이 파트너 API 경로
export const TOSSPAY_MAKE_PAYMENT = "/api-partner/v1/apps-in-toss/pay/make-payment"
export const TOSSPAY_EXECUTE_PAYMENT = "/api-partner/v1/apps-in-toss/pay/execute-payment"

export function matchFeeProductDesc(): string {
  return "탁카 매칭 이용료"
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/apps-in-toss/matchFeeConfig.ts
git commit -m "feat: 매칭 이용료 상수(금액·TTL·토스경로)"
```

---

### Task 3: 토스페이 순수 호출 (tossPay.ts) — TDD

토스페이 make-payment/execute-payment를 주입된 fetch로만 호출(tossLogin.ts 패턴). mTLS·헤더는 호출부가 fetch에 주입.

**Files:**
- Create: `lib/apps-in-toss/tossPay.ts`
- Test: `lib/apps-in-toss/tossPay.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
import { describe, it, expect } from "vitest"
import { createTossPayment, executeTossPayment } from "./tossPay"

function okFetch(json: unknown): typeof fetch {
  return (async () => ({ ok: true, status: 200, json: async () => json })) as unknown as typeof fetch
}
function failFetch(status: number): typeof fetch {
  return (async () => ({ ok: false, status, json: async () => ({}) })) as unknown as typeof fetch
}

describe("createTossPayment", () => {
  it("성공 시 payToken 반환", async () => {
    const f = okFetch({ resultType: "SUCCESS", success: { payToken: "pt_1" } })
    const r = await createTossPayment(f, { apiBase: "https://x" }, {
      tossUserKey: "uk_1", orderNo: "o-1", amount: 3000, productDesc: "탁카 매칭 이용료", isTest: true,
    })
    expect(r).toEqual({ payToken: "pt_1" })
  })
  it("실패 응답이면 에러", async () => {
    await expect(createTossPayment(failFetch(400), { apiBase: "https://x" }, {
      tossUserKey: "uk_1", orderNo: "o-1", amount: 3000, productDesc: "d", isTest: true,
    })).rejects.toThrow("결제 생성 실패: 400")
  })
  it("resultType이 SUCCESS 아니면 에러", async () => {
    const f = okFetch({ resultType: "FAIL", success: null })
    await expect(createTossPayment(f, { apiBase: "https://x" }, {
      tossUserKey: "uk_1", orderNo: "o-1", amount: 3000, productDesc: "d", isTest: true,
    })).rejects.toThrow("결제 생성 실패")
  })
})

describe("executeTossPayment", () => {
  it("성공 시 승인결과 반환", async () => {
    const f = okFetch({ resultType: "SUCCESS", success: { stateMsg: "결제 완료", transactionId: "tx_1", payToken: "pt_1" } })
    const r = await executeTossPayment(f, { apiBase: "https://x" }, {
      tossUserKey: "uk_1", payToken: "pt_1", orderNo: "o-1", isTest: true,
    })
    expect(r).toEqual({ stateMsg: "결제 완료", transactionId: "tx_1", payToken: "pt_1" })
  })
  it("실패면 에러", async () => {
    await expect(executeTossPayment(failFetch(500), { apiBase: "https://x" }, {
      tossUserKey: "uk_1", payToken: "pt_1", orderNo: "o-1", isTest: true,
    })).rejects.toThrow("결제 승인 실패: 500")
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run lib/apps-in-toss/tossPay.test.ts`
Expected: FAIL — `createTossPayment`/`executeTossPayment` not defined.

- [ ] **Step 3: 구현 작성**

```ts
// 토스페이 파트너 API 순수 호출. mTLS·헤더는 fetchFn에 주입(tossLogin.ts 동일 패턴).
import { TOSSPAY_MAKE_PAYMENT, TOSSPAY_EXECUTE_PAYMENT } from "./matchFeeConfig"

export interface TossPayConfig {
  apiBase: string
}

export interface CreatePaymentInput {
  tossUserKey: string
  orderNo: string
  amount: number
  productDesc: string
  isTest: boolean
}

export async function createTossPayment(
  fetchFn: typeof fetch,
  cfg: TossPayConfig,
  input: CreatePaymentInput
): Promise<{ payToken: string }> {
  const res = await fetchFn(`${cfg.apiBase}${TOSSPAY_MAKE_PAYMENT}`, {
    method: "POST",
    headers: { "Content-Type": "application/json", "x-toss-user-key": input.tossUserKey },
    body: JSON.stringify({
      orderNo: input.orderNo,
      productDesc: input.productDesc,
      amount: input.amount,
      amountTaxFree: input.amount, // 중개 서비스료: 비과세 처리(과세 필요 시 정책에 맞게 조정)
      cashReceipt: false,
      isTestPayment: input.isTest,
    }),
  })
  if (!res.ok) throw new Error(`결제 생성 실패: ${res.status}`)
  const json = await res.json()
  if (json?.resultType !== "SUCCESS" || !json?.success?.payToken) {
    throw new Error(`결제 생성 실패: ${json?.error?.errorCode ?? "unknown"}`)
  }
  return { payToken: json.success.payToken as string }
}

export interface ExecutePaymentInput {
  tossUserKey: string
  payToken: string
  orderNo: string
  isTest: boolean
}

export async function executeTossPayment(
  fetchFn: typeof fetch,
  cfg: TossPayConfig,
  input: ExecutePaymentInput
): Promise<{ stateMsg: string; transactionId: string; payToken: string }> {
  const res = await fetchFn(`${cfg.apiBase}${TOSSPAY_EXECUTE_PAYMENT}`, {
    method: "POST",
    headers: { "Content-Type": "application/json", "x-toss-user-key": input.tossUserKey },
    body: JSON.stringify({ payToken: input.payToken, orderNo: input.orderNo, isTestPayment: input.isTest }),
  })
  if (!res.ok) throw new Error(`결제 승인 실패: ${res.status}`)
  const json = await res.json()
  if (json?.resultType !== "SUCCESS") {
    throw new Error(`결제 승인 실패: ${json?.success?.errorCode ?? json?.error?.errorCode ?? "unknown"}`)
  }
  const s = json.success
  return { stateMsg: s.stateMsg, transactionId: s.transactionId, payToken: s.payToken }
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run lib/apps-in-toss/tossPay.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: Commit**

```bash
git add lib/apps-in-toss/tossPay.ts lib/apps-in-toss/tossPay.test.ts
git commit -m "feat: 토스페이 make/execute 순수 호출(DI fetch, TDD)"
```

---

### Task 4: 매칭 생성 시 fee 생성 (approveBid 확장) — TDD

**Files:**
- Modify: `lib/apps-in-toss/approveBid.ts`
- Modify: `lib/apps-in-toss/approveBid.test.ts`

- [ ] **Step 1: 테스트 추가**

`lib/apps-in-toss/approveBid.test.ts`의 `deps()` 헬퍼에 `insertMatchFee` 추가하고, 정상 흐름 테스트의 기대 호출 순서를 갱신한다. 기존 `deps` 함수 안 `d` 객체에 아래 줄 추가:

```ts
      insertMatchFee: async () => { calls.push("fee") },
```

그리고 첫 테스트의 기대 배열을 변경:

```ts
    expect(calls).toEqual(["match", "order", "accept", "reject", "fee"])
```

새 테스트 추가:

```ts
  it("fee 생성은 match·order·accept·reject 이후 마지막에 호출", async () => {
    const { d, calls } = deps()
    await approveBidFlow(d, { bidId: "b1", orderId: "o1", userId: "shipper-1" })
    expect(calls[calls.length - 1]).toBe("fee")
  })
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run lib/apps-in-toss/approveBid.test.ts`
Expected: FAIL — `insertMatchFee` 미정의 / 호출 순서 불일치.

- [ ] **Step 3: 구현 — approveBid.ts 수정**

`ApproveBidDeps` 인터페이스에 추가:

```ts
  insertMatchFee: (orderId: string, driverId: string) => Promise<void>
```

`approveBidFlow` 함수의 `await deps.rejectOtherBids(...)` 다음에 추가:

```ts
  await deps.insertMatchFee(input.orderId, bid.driver_id)
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run lib/apps-in-toss/approveBid.test.ts`
Expected: PASS

- [ ] **Step 5: 라우트 배선 — approve/route.ts**

`app/api/apps-in-toss/bids/approve/route.ts`의 `approveBidFlow` deps 객체에 아래를 추가(다른 dep들과 같은 위치). `MATCH_FEE_AMOUNT`, `MATCH_FEE_TTL_MS` import 추가:

```ts
import { MATCH_FEE_AMOUNT, MATCH_FEE_TTL_MS } from "@/lib/apps-in-toss/matchFeeConfig"
```

deps 객체 안:

```ts
        insertMatchFee: async (orderId, driverId) => {
          const { data: match } = await service.from("matches")
            .select("id").eq("order_id", orderId).eq("driver_id", driverId).single()
          if (!match) return
          await service.from("match_fees").upsert({
            match_id: match.id,
            driver_id: driverId,
            amount: MATCH_FEE_AMOUNT,
            status: "pending",
            expires_at: new Date(Date.now() + MATCH_FEE_TTL_MS).toISOString(),
          }, { onConflict: "match_id", ignoreDuplicates: true })
        },
```

- [ ] **Step 6: 타입체크 + Commit**

Run: `npx tsc --noEmit`
Expected: 에러 없음

```bash
git add lib/apps-in-toss/approveBid.ts lib/apps-in-toss/approveBid.test.ts app/api/apps-in-toss/bids/approve/route.ts
git commit -m "feat: 매칭 수락 시 match_fee(pending, 24h) 생성"
```

---

### Task 5: 결제 오케스트레이터 (matchFeeFlow) — TDD

payMatchFee(검증→payToken), confirmMatchFee(승인→paid), expireMatchFee(만료 정리)를 주입형으로.

**Files:**
- Create: `lib/apps-in-toss/matchFeeFlow.ts`
- Test: `lib/apps-in-toss/matchFeeFlow.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
import { describe, it, expect } from "vitest"
import { payMatchFeeFlow, confirmMatchFeeFlow, expireMatchFeeFlow } from "./matchFeeFlow"

const now = () => new Date("2026-07-10T00:00:00Z").getTime()
const future = new Date("2026-07-11T00:00:00Z").toISOString()
const past = new Date("2026-07-09T00:00:00Z").toISOString()

function payDeps(over = {}) {
  const calls: string[] = []
  return {
    calls,
    d: {
      getFee: async () => ({ id: "f1", driver_id: "d1", amount: 3000, status: "pending", expires_at: future, toss_order_no: null as string | null }),
      getDriverTossKey: async () => "uk_1",
      setOrderNo: async () => { calls.push("orderNo") },
      createPayment: async () => { calls.push("create"); return { payToken: "pt_1" } },
      now,
      ...over,
    },
  }
}

describe("payMatchFeeFlow", () => {
  it("pending·미만료·본인이면 payToken 발급", async () => {
    const { d, calls } = payDeps()
    const r = await payMatchFeeFlow(d, { feeId: "f1", userId: "d1", isTest: true })
    expect(r).toEqual({ payToken: "pt_1" })
    expect(calls).toContain("create")
  })
  it("본인 아니면 권한 에러", async () => {
    const { d } = payDeps()
    await expect(payMatchFeeFlow(d, { feeId: "f1", userId: "other", isTest: true }))
      .rejects.toThrow("권한이 없습니다")
  })
  it("이미 결제(paid)면 에러", async () => {
    const { d } = payDeps({ getFee: async () => ({ id: "f1", driver_id: "d1", amount: 3000, status: "paid", expires_at: future, toss_order_no: "o" }) })
    await expect(payMatchFeeFlow(d, { feeId: "f1", userId: "d1", isTest: true }))
      .rejects.toThrow("이미 결제된 매칭입니다")
  })
  it("만료면 에러", async () => {
    const { d } = payDeps({ getFee: async () => ({ id: "f1", driver_id: "d1", amount: 3000, status: "pending", expires_at: past, toss_order_no: null }) })
    await expect(payMatchFeeFlow(d, { feeId: "f1", userId: "d1", isTest: true }))
      .rejects.toThrow("매칭이 만료되었습니다")
  })
})

function confirmDeps(over = {}) {
  const calls: string[] = []
  return {
    calls,
    d: {
      getFee: async () => ({ id: "f1", driver_id: "d1", status: "pending", toss_order_no: "o-1" }),
      getDriverTossKey: async () => "uk_1",
      executePayment: async () => { calls.push("execute"); return { transactionId: "tx_1", payToken: "pt_1", stateMsg: "결제 완료" } },
      markPaid: async () => { calls.push("paid") },
      ...over,
    },
  }
}

describe("confirmMatchFeeFlow", () => {
  it("승인 성공 시 paid 마킹", async () => {
    const { d, calls } = confirmDeps()
    const r = await confirmMatchFeeFlow(d, { feeId: "f1", userId: "d1", payToken: "pt_1", isTest: true })
    expect(r).toEqual({ ok: true })
    expect(calls).toEqual(["execute", "paid"])
  })
  it("본인 아니면 권한 에러", async () => {
    const { d } = confirmDeps()
    await expect(confirmMatchFeeFlow(d, { feeId: "f1", userId: "x", payToken: "pt_1", isTest: true }))
      .rejects.toThrow("권한이 없습니다")
  })
})

function expireDeps(over = {}) {
  const calls: string[] = []
  return {
    calls,
    d: {
      getFee: async () => ({ id: "f1", match_id: "m1", status: "pending", expires_at: past }),
      getMatchOrder: async () => ({ order_id: "o1" }),
      cancelMatch: async () => { calls.push("cancelMatch") },
      reopenOrder: async () => { calls.push("reopen") },
      cancelFee: async () => { calls.push("cancelFee") },
      now,
      ...over,
    },
  }
}

describe("expireMatchFeeFlow", () => {
  it("만료+pending이면 매칭 취소·주문 재오픈·fee 취소", async () => {
    const { d, calls } = expireDeps()
    const r = await expireMatchFeeFlow(d, { feeId: "f1" })
    expect(r).toEqual({ expired: true })
    expect(calls).toEqual(["cancelMatch", "reopen", "cancelFee"])
  })
  it("미만료면 아무것도 안 함", async () => {
    const { d, calls } = expireDeps({ getFee: async () => ({ id: "f1", match_id: "m1", status: "pending", expires_at: future }) })
    const r = await expireMatchFeeFlow(d, { feeId: "f1" })
    expect(r).toEqual({ expired: false })
    expect(calls).toEqual([])
  })
  it("이미 paid면 아무것도 안 함", async () => {
    const { d, calls } = expireDeps({ getFee: async () => ({ id: "f1", match_id: "m1", status: "paid", expires_at: past }) })
    const r = await expireMatchFeeFlow(d, { feeId: "f1" })
    expect(r).toEqual({ expired: false })
    expect(calls).toEqual([])
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run lib/apps-in-toss/matchFeeFlow.test.ts`
Expected: FAIL — 함수 미정의.

- [ ] **Step 3: 구현 작성**

```ts
// 매칭 이용료 오케스트레이터(주입형). 토스페이·Supabase는 라우트가 주입.

export interface PayDeps {
  getFee: (feeId: string) => Promise<{ id: string; driver_id: string; amount: number; status: string; expires_at: string; toss_order_no: string | null } | null>
  getDriverTossKey: (driverId: string) => Promise<string | null>
  setOrderNo: (feeId: string, orderNo: string) => Promise<void>
  createPayment: (args: { tossUserKey: string; orderNo: string; amount: number; isTest: boolean }) => Promise<{ payToken: string }>
  now: () => number
}

export async function payMatchFeeFlow(
  deps: PayDeps,
  input: { feeId: string; userId: string; isTest: boolean }
): Promise<{ payToken: string }> {
  const fee = await deps.getFee(input.feeId)
  if (!fee) throw new Error("이용료 정보를 찾을 수 없습니다")
  if (fee.driver_id !== input.userId) throw new Error("권한이 없습니다")
  if (fee.status === "paid") throw new Error("이미 결제된 매칭입니다")
  if (fee.status !== "pending") throw new Error("결제할 수 없는 상태입니다")
  if (new Date(fee.expires_at).getTime() < deps.now()) throw new Error("매칭이 만료되었습니다")

  const tossUserKey = await deps.getDriverTossKey(fee.driver_id)
  if (!tossUserKey) throw new Error("토스 사용자 정보가 없습니다")

  // 유니크 주문번호: fee.id + timestamp (재시도 시 재사용 불가하므로 매번 신규)
  const orderNo = `mf_${fee.id.replace(/-/g, "").slice(0, 20)}_${deps.now()}`
  await deps.setOrderNo(input.feeId, orderNo)
  const { payToken } = await deps.createPayment({ tossUserKey, orderNo, amount: fee.amount, isTest: input.isTest })
  return { payToken }
}

export interface ConfirmDeps {
  getFee: (feeId: string) => Promise<{ id: string; driver_id: string; status: string; toss_order_no: string | null } | null>
  getDriverTossKey: (driverId: string) => Promise<string | null>
  executePayment: (args: { tossUserKey: string; payToken: string; orderNo: string; isTest: boolean }) => Promise<{ transactionId: string; payToken: string; stateMsg: string }>
  markPaid: (feeId: string, payToken: string) => Promise<void>
}

export async function confirmMatchFeeFlow(
  deps: ConfirmDeps,
  input: { feeId: string; userId: string; payToken: string; isTest: boolean }
): Promise<{ ok: true }> {
  const fee = await deps.getFee(input.feeId)
  if (!fee) throw new Error("이용료 정보를 찾을 수 없습니다")
  if (fee.driver_id !== input.userId) throw new Error("권한이 없습니다")
  if (fee.status === "paid") return { ok: true } // 멱등
  if (!fee.toss_order_no) throw new Error("결제 생성 이력이 없습니다")

  const tossUserKey = await deps.getDriverTossKey(fee.driver_id)
  if (!tossUserKey) throw new Error("토스 사용자 정보가 없습니다")

  await deps.executePayment({ tossUserKey, payToken: input.payToken, orderNo: fee.toss_order_no, isTest: input.isTest })
  await deps.markPaid(input.feeId, input.payToken)
  return { ok: true }
}

export interface ExpireDeps {
  getFee: (feeId: string) => Promise<{ id: string; match_id: string; status: string; expires_at: string } | null>
  getMatchOrder: (matchId: string) => Promise<{ order_id: string } | null>
  cancelMatch: (matchId: string) => Promise<void>
  reopenOrder: (orderId: string) => Promise<void>
  cancelFee: (feeId: string) => Promise<void>
  now: () => number
}

export async function expireMatchFeeFlow(
  deps: ExpireDeps,
  input: { feeId: string }
): Promise<{ expired: boolean }> {
  const fee = await deps.getFee(input.feeId)
  if (!fee) return { expired: false }
  if (fee.status !== "pending") return { expired: false }
  if (new Date(fee.expires_at).getTime() >= deps.now()) return { expired: false }

  const mo = await deps.getMatchOrder(fee.match_id)
  await deps.cancelMatch(fee.match_id)
  if (mo) await deps.reopenOrder(mo.order_id)
  await deps.cancelFee(fee.id)
  return { expired: true }
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run lib/apps-in-toss/matchFeeFlow.test.ts`
Expected: PASS (9 tests)

- [ ] **Step 5: Commit**

```bash
git add lib/apps-in-toss/matchFeeFlow.ts lib/apps-in-toss/matchFeeFlow.test.ts
git commit -m "feat: 매칭이용료 pay/confirm/expire 오케스트레이터(주입형 TDD)"
```

---

### Task 6: 라우트 배선 (create/confirm/expire)

토스페이 호출은 mTLS fetch(`createMtlsFetch`, lib/apps-in-toss/mtls.ts)를 주입. env: `TOSS_PARTNER_API_BASE`, `TOSS_MTLS_CERT`, `TOSS_MTLS_KEY`(login과 공용).

**Files:**
- Create: `app/api/apps-in-toss/match-fee/create/route.ts`
- Create: `app/api/apps-in-toss/match-fee/confirm/route.ts`
- Create: `app/api/apps-in-toss/match-fee/expire/route.ts`

- [ ] **Step 1: create 라우트 작성**

```ts
import { NextResponse } from "next/server"
import type { NextRequest } from "next/server"
import { createClient } from "@supabase/supabase-js"
import { createServiceClient } from "@/lib/supabase/service"
import { createMtlsFetch } from "@/lib/apps-in-toss/mtls"
import { payMatchFeeFlow } from "@/lib/apps-in-toss/matchFeeFlow"
import { createTossPayment } from "@/lib/apps-in-toss/tossPay"
import { matchFeeProductDesc } from "@/lib/apps-in-toss/matchFeeConfig"

export async function POST(request: NextRequest) {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL
  const anon = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
  const apiBase = process.env.TOSS_PARTNER_API_BASE
  const cert = process.env.TOSS_MTLS_CERT
  const key = process.env.TOSS_MTLS_KEY
  if (!url || !anon || !apiBase || !cert || !key) {
    return NextResponse.json({ error: "server not configured" }, { status: 503 })
  }
  const token = request.headers.get("authorization")?.replace("Bearer ", "")
  if (!token) return NextResponse.json({ error: "unauthorized" }, { status: 401 })
  const userClient = createClient(url, anon, {
    global: { headers: { Authorization: `Bearer ${token}` } },
    auth: { persistSession: false, autoRefreshToken: false },
  })
  const { data: { user } } = await userClient.auth.getUser()
  if (!user) return NextResponse.json({ error: "unauthorized" }, { status: 401 })

  let body: { feeId?: string; isTest?: boolean }
  try { body = await request.json() } catch { return NextResponse.json({ error: "invalid body" }, { status: 400 }) }
  if (!body.feeId) return NextResponse.json({ error: "feeId 필요" }, { status: 400 })

  const service = createServiceClient()
  const mtlsFetch = createMtlsFetch(cert, key)
  try {
    const result = await payMatchFeeFlow(
      {
        getFee: async (feeId) => {
          const { data } = await service.from("match_fees")
            .select("id,driver_id,amount,status,expires_at,toss_order_no").eq("id", feeId).single()
          return data as Parameters<typeof payMatchFeeFlow>[0]["getFee"] extends never ? never : any
        },
        getDriverTossKey: async (driverId) => {
          const { data } = await service.from("users").select("toss_user_key").eq("id", driverId).single()
          return data?.toss_user_key ?? null
        },
        setOrderNo: async (feeId, orderNo) => {
          await service.from("match_fees").update({ toss_order_no: orderNo }).eq("id", feeId)
        },
        createPayment: async ({ tossUserKey, orderNo, amount, isTest }) =>
          createTossPayment(mtlsFetch, { apiBase }, { tossUserKey, orderNo, amount, productDesc: matchFeeProductDesc(), isTest }),
        now: () => Date.now(),
      },
      { feeId: body.feeId, userId: user.id, isTest: body.isTest ?? false }
    )
    return NextResponse.json(result)
  } catch (e) {
    return NextResponse.json({ error: e instanceof Error ? e.message : "create failed" }, { status: 400 })
  }
}
```

> 참고: `getFee`의 반환 캐스팅은 위 `any`를 실제 타입으로 좁혀도 되지만, service 쿼리 결과가 오케스트레이터 인터페이스와 필드가 일치하므로 `as any` 대신 명시 캐스팅 사용 권장:
> `return data as { id: string; driver_id: string; amount: number; status: string; expires_at: string; toss_order_no: string | null } | null`

- [ ] **Step 2: create 라우트 getFee 캐스팅 정리**

위 `getFee` 본문을 아래로 교체(타입 명시):

```ts
        getFee: async (feeId) => {
          const { data } = await service.from("match_fees")
            .select("id,driver_id,amount,status,expires_at,toss_order_no").eq("id", feeId).single()
          return (data as { id: string; driver_id: string; amount: number; status: string; expires_at: string; toss_order_no: string | null } | null) ?? null
        },
```

- [ ] **Step 3: confirm 라우트 작성**

```ts
import { NextResponse } from "next/server"
import type { NextRequest } from "next/server"
import { createClient } from "@supabase/supabase-js"
import { createServiceClient } from "@/lib/supabase/service"
import { createMtlsFetch } from "@/lib/apps-in-toss/mtls"
import { confirmMatchFeeFlow } from "@/lib/apps-in-toss/matchFeeFlow"
import { executeTossPayment } from "@/lib/apps-in-toss/tossPay"

export async function POST(request: NextRequest) {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL
  const anon = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
  const apiBase = process.env.TOSS_PARTNER_API_BASE
  const cert = process.env.TOSS_MTLS_CERT
  const key = process.env.TOSS_MTLS_KEY
  if (!url || !anon || !apiBase || !cert || !key) {
    return NextResponse.json({ error: "server not configured" }, { status: 503 })
  }
  const token = request.headers.get("authorization")?.replace("Bearer ", "")
  if (!token) return NextResponse.json({ error: "unauthorized" }, { status: 401 })
  const userClient = createClient(url, anon, {
    global: { headers: { Authorization: `Bearer ${token}` } },
    auth: { persistSession: false, autoRefreshToken: false },
  })
  const { data: { user } } = await userClient.auth.getUser()
  if (!user) return NextResponse.json({ error: "unauthorized" }, { status: 401 })

  let body: { feeId?: string; payToken?: string; isTest?: boolean }
  try { body = await request.json() } catch { return NextResponse.json({ error: "invalid body" }, { status: 400 }) }
  if (!body.feeId || !body.payToken) return NextResponse.json({ error: "feeId/payToken 필요" }, { status: 400 })

  const service = createServiceClient()
  const mtlsFetch = createMtlsFetch(cert, key)
  try {
    const result = await confirmMatchFeeFlow(
      {
        getFee: async (feeId) => {
          const { data } = await service.from("match_fees").select("id,driver_id,status,toss_order_no").eq("id", feeId).single()
          return (data as { id: string; driver_id: string; status: string; toss_order_no: string | null } | null) ?? null
        },
        getDriverTossKey: async (driverId) => {
          const { data } = await service.from("users").select("toss_user_key").eq("id", driverId).single()
          return data?.toss_user_key ?? null
        },
        executePayment: async ({ tossUserKey, payToken, orderNo, isTest }) =>
          executeTossPayment(mtlsFetch, { apiBase }, { tossUserKey, payToken, orderNo, isTest }),
        markPaid: async (feeId, payToken) => {
          await service.from("match_fees").update({ status: "paid", toss_payment_key: payToken, paid_at: new Date().toISOString() }).eq("id", feeId)
        },
      },
      { feeId: body.feeId, userId: user.id, payToken: body.payToken, isTest: body.isTest ?? false }
    )
    return NextResponse.json(result)
  } catch (e) {
    return NextResponse.json({ error: e instanceof Error ? e.message : "confirm failed" }, { status: 400 })
  }
}
```

- [ ] **Step 4: expire 라우트 작성**

```ts
import { NextResponse } from "next/server"
import type { NextRequest } from "next/server"
import { createClient } from "@supabase/supabase-js"
import { createServiceClient } from "@/lib/supabase/service"
import { expireMatchFeeFlow } from "@/lib/apps-in-toss/matchFeeFlow"

export async function POST(request: NextRequest) {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL
  const anon = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
  if (!url || !anon) return NextResponse.json({ error: "server not configured" }, { status: 503 })
  const token = request.headers.get("authorization")?.replace("Bearer ", "")
  if (!token) return NextResponse.json({ error: "unauthorized" }, { status: 401 })
  const userClient = createClient(url, anon, {
    global: { headers: { Authorization: `Bearer ${token}` } },
    auth: { persistSession: false, autoRefreshToken: false },
  })
  const { data: { user } } = await userClient.auth.getUser()
  if (!user) return NextResponse.json({ error: "unauthorized" }, { status: 401 })

  let body: { feeId?: string }
  try { body = await request.json() } catch { return NextResponse.json({ error: "invalid body" }, { status: 400 }) }
  if (!body.feeId) return NextResponse.json({ error: "feeId 필요" }, { status: 400 })

  const service = createServiceClient()
  try {
    const result = await expireMatchFeeFlow(
      {
        getFee: async (feeId) => {
          const { data } = await service.from("match_fees").select("id,match_id,status,expires_at").eq("id", feeId).single()
          return (data as { id: string; match_id: string; status: string; expires_at: string } | null) ?? null
        },
        getMatchOrder: async (matchId) => {
          const { data } = await service.from("matches").select("order_id").eq("id", matchId).single()
          return (data as { order_id: string } | null) ?? null
        },
        cancelMatch: async (matchId) => { await service.from("matches").update({ status: "cancelled" }).eq("id", matchId) },
        reopenOrder: async (orderId) => {
          await service.from("orders").update({ status: "pending", price: 0 }).eq("id", orderId)
          await service.from("bids").update({ status: "pending" }).eq("order_id", orderId).eq("status", "accepted")
        },
        cancelFee: async (feeId) => { await service.from("match_fees").update({ status: "cancelled" }).eq("id", feeId) },
        now: () => Date.now(),
      },
      { feeId: body.feeId }
    )
    return NextResponse.json(result)
  } catch (e) {
    return NextResponse.json({ error: e instanceof Error ? e.message : "expire failed" }, { status: 400 })
  }
}
```

> 주의: `reopenOrder`의 `price: 0` 재설정은 "다시 pending → 재입찰" 정책상 확정가를 초기화한다. 원 희망가를 보존하려면 orders에 `desired_price` 별도 컬럼이 필요하나, 현재 스키마는 price 단일 컬럼이므로 재오픈 시 0으로 두고 화주가 재등록하도록 한다(YAGNI). 이 동작이 마음에 걸리면 별도 후속 작업으로 desired_price 분리.

- [ ] **Step 5: 타입체크 + Commit**

Run: `npx tsc --noEmit`
Expected: 에러 없음

```bash
git add app/api/apps-in-toss/match-fee/
git commit -m "feat: 매칭이용료 create/confirm/expire 라우트(mTLS 토스페이 배선)"
```

---

### Task 7: MatchDetail 게이팅 + 결제 CTA (프론트)

**Files:**
- Modify: `apps-in-toss/src/screens/MatchDetail.tsx`

- [ ] **Step 1: fee 상태 로드 + 게이팅 추가**

`MatchDetail.tsx` 상단 상태에 추가:

```ts
  const [fee, setFee] = useState<{ id: string; status: string; amount: number; expires_at: string } | null>(null)
  const [payMsg, setPayMsg] = useState("")
  const [paying, setPaying] = useState(false)
```

`load` useEffect 안, match 해소 직후(`setMatchId(match.id)` 다음)에 fee 조회 추가:

```ts
      const { data: feeRow } = await supabase.from("match_fees")
        .select("id,status,amount,expires_at").eq("match_id", match.id).maybeSingle()
      setFee(feeRow as { id: string; status: string; amount: number; expires_at: string } | null)
```

파일 상단 import 추가:

```ts
import { checkoutPayment } from "@apps-in-toss/web-framework"
```

`BACKEND` 상수 추가(파일 상단, ORANGE 옆):

```ts
const BACKEND = import.meta.env.VITE_BACKEND_URL ?? "https://takca.vercel.app"
```

- [ ] **Step 2: 결제 핸들러 추가**

컴포넌트 함수 내에 추가:

```ts
  const isPaid = fee?.status === "paid"
  const isDriver = meId != null && other != null && myPhoneKnownAsDriver()

  async function payFee() {
    if (!fee) return
    setPaying(true); setPayMsg("결제 준비 중…")
    const { data: { session } } = await supabase.auth.getSession()
    if (!session) { setPayMsg("로그인이 필요해요"); setPaying(false); return }
    try {
      const createRes = await fetch(`${BACKEND}/api/apps-in-toss/match-fee/create`, {
        method: "POST",
        headers: { "Content-Type": "application/json", Authorization: `Bearer ${session.access_token}` },
        body: JSON.stringify({ feeId: fee.id, isTest: false }),
      })
      if (!createRes.ok) { const j = await createRes.json().catch(() => ({})); setPayMsg(`결제 생성 실패: ${j.error ?? createRes.status}`); setPaying(false); return }
      const { payToken } = await createRes.json()
      const { success, reason } = await checkoutPayment({ payToken })
      if (!success) { setPayMsg(`결제 취소: ${reason ?? ""}`); setPaying(false); return }
      const confirmRes = await fetch(`${BACKEND}/api/apps-in-toss/match-fee/confirm`, {
        method: "POST",
        headers: { "Content-Type": "application/json", Authorization: `Bearer ${session.access_token}` },
        body: JSON.stringify({ feeId: fee.id, payToken, isTest: false }),
      })
      if (!confirmRes.ok) { const j = await confirmRes.json().catch(() => ({})); setPayMsg(`결제 승인 실패: ${j.error ?? confirmRes.status}`); setPaying(false); return }
      setPayMsg("결제 완료 ✅ 연락처가 열렸어요")
      setFee({ ...fee, status: "paid" })
    } catch (e) {
      setPayMsg(`오류: ${e instanceof Error ? e.message : String(e)}`)
    } finally { setPaying(false) }
  }
```

> `myPhoneKnownAsDriver`는 실제로 필요 없다. 뷰어가 기사인지 판별은 `meId === match.driver_id`로 하되, MatchDetail의 기존 load에서 `match.driver_id`를 상태로 보존해야 한다. Step 3에서 처리.

- [ ] **Step 3: 뷰어 역할(기사 여부) 상태 보존**

`load` useEffect에서 `const otherId = ...` 직전에 driver 여부 저장. 상태 추가:

```ts
  const [iAmDriver, setIAmDriver] = useState(false)
```

`otherId` 계산부를 아래로 교체:

```ts
      const amDriver = user.id === match.driver_id
      setIAmDriver(amDriver)
      const otherId = amDriver ? order.shipper_id : match.driver_id
```

Step 2에서 넣은 `isDriver`/`myPhoneKnownAsDriver` 줄은 삭제하고, 게이팅에 `iAmDriver`/`isPaid`를 사용한다.

- [ ] **Step 4: 연락처·채팅 잠금 렌더**

상대 카드와 채팅 블록을 `isPaid` 게이팅으로 감싼다. 상대 카드 렌더 조건을 `other && isPaid`로 바꾸고, 미결제 시 아래 블록을 대신 렌더:

```tsx
      {other && !isPaid && (
        <div style={{ background: "#FFF7ED", border: "1.5px solid #FED7AA", borderRadius: 16, padding: 18, marginTop: 18 }}>
          {iAmDriver ? (
            <>
              <div style={{ fontSize: 15, fontWeight: 800, color: "#9A3412" }}>매칭 성사! 이용료를 결제하면 화주 연락처와 채팅이 열려요</div>
              <div style={{ color: "#6B7280", fontSize: 14, margin: "8px 0 14px" }}>
                매칭 이용료 {(fee?.amount ?? 3000).toLocaleString()}원 · 미결제 시 24시간 후 매칭이 자동 취소돼요
              </div>
              <button onClick={payFee} disabled={paying}
                style={{ width: "100%", background: ORANGE, color: "#fff", border: 0, borderRadius: 12, padding: 16, fontSize: 16, fontWeight: 800, cursor: paying ? "default" : "pointer", opacity: paying ? 0.6 : 1 }}>
                {(fee?.amount ?? 3000).toLocaleString()}원 결제하고 연락처 받기
              </button>
              <div style={{ color: "#9CA3AF", fontSize: 13, minHeight: 18, marginTop: 8, textAlign: "center" }}>{payMsg}</div>
            </>
          ) : (
            <div style={{ fontSize: 15, fontWeight: 700, color: "#9A3412" }}>기사님이 매칭을 확정하면 연락처가 열려요</div>
          )}
        </div>
      )}
```

채팅 블록(제목 `<h3>채팅</h3>`부터 입력창까지)은 `{isPaid && ( ... )}`로 감싼다.

- [ ] **Step 5: 빌드 검증**

Run: `cd apps-in-toss && npx tsc -p tsconfig.app.json --noEmit && VITE_SUPABASE_URL=https://x VITE_SUPABASE_ANON_KEY=dummy npx vite build`
Expected: 타입체크 통과 + 빌드 성공

- [ ] **Step 6: Commit**

```bash
git add apps-in-toss/src/screens/MatchDetail.tsx
git commit -m "feat: MatchDetail 매칭이용료 게이팅 + 토스페이 결제 CTA"
```

---

### Task 8: 약관/개인정보 조항 (More.tsx)

**Files:**
- Modify: `apps-in-toss/src/screens/More.tsx`

- [ ] **Step 1: 이용약관 섹션에 매칭 이용료·환불 조항 추가**

`More.tsx`의 이용약관 `Section` 내용(제6조 다음)에 아래 문자열을 추가:

```
제7조(매칭 이용료) ① 회사는 매칭 성사 시 기사 회원에게 매칭 이용료(건당 3,000원)를 부과하며, 토스페이로 결제합니다. ② 화주에게는 매칭 이용료가 부과되지 않습니다. ③ 매칭 이용료는 탁송 대금과 무관한 회사의 중개 서비스 이용 대가입니다.

제8조(환불) ① 매칭 이용료 결제 후 상대방 연락처 열람 및 채팅 이용 전에는 전액 환불합니다. ② 연락처 열람 이후에는 서비스가 제공된 것으로 보아 환불이 제한될 수 있습니다. ③ 환불 문의는 고객센터로 접수해 주세요.
```

- [ ] **Step 2: 개인정보처리방침 섹션에 결제 항목 추가**

개인정보처리방침 `Section`의 "1. 수집 항목"에 다음을 덧붙임(문자열 내):

```
결제 정보(토스페이 결제 토큰·거래 식별자 — 매칭 이용료 결제 시).
```

- [ ] **Step 3: 빌드 + Commit**

Run: `cd apps-in-toss && npx vite build`
Expected: 성공

```bash
git add apps-in-toss/src/screens/More.tsx
git commit -m "docs: 약관 매칭이용료·환불 조항 + 개인정보 결제항목"
```

---

### Task 9: 만료 스윕 크론 백스톱

lazy 정리(조회 시점)가 주 경로지만, 아무도 안 여는 매칭을 위해 크론으로 만료 fee를 스윕한다.

**Files:**
- Create: `app/api/apps-in-toss/match-fee/sweep/route.ts`
- Modify: `vercel.json`

- [ ] **Step 1: sweep 라우트 작성**

```ts
import { NextResponse } from "next/server"
import type { NextRequest } from "next/server"
import { createServiceClient } from "@/lib/supabase/service"
import { expireMatchFeeFlow } from "@/lib/apps-in-toss/matchFeeFlow"

export async function GET(request: NextRequest) {
  const secret = process.env.CRON_SECRET
  const auth = request.headers.get("authorization")
  if (process.env.NODE_ENV === "production" && (!secret || auth !== `Bearer ${secret}`)) {
    return NextResponse.json({ error: "unauthorized" }, { status: 401 })
  }
  const service = createServiceClient()
  const { data: expired } = await service.from("match_fees")
    .select("id").eq("status", "pending").lt("expires_at", new Date().toISOString()).limit(100)
  let count = 0
  for (const row of (expired as { id: string }[] ?? [])) {
    await expireMatchFeeFlow(
      {
        getFee: async (feeId) => {
          const { data } = await service.from("match_fees").select("id,match_id,status,expires_at").eq("id", feeId).single()
          return (data as { id: string; match_id: string; status: string; expires_at: string } | null) ?? null
        },
        getMatchOrder: async (matchId) => {
          const { data } = await service.from("matches").select("order_id").eq("id", matchId).single()
          return (data as { order_id: string } | null) ?? null
        },
        cancelMatch: async (matchId) => { await service.from("matches").update({ status: "cancelled" }).eq("id", matchId) },
        reopenOrder: async (orderId) => {
          await service.from("orders").update({ status: "pending", price: 0 }).eq("id", orderId)
          await service.from("bids").update({ status: "pending" }).eq("order_id", orderId).eq("status", "accepted")
        },
        cancelFee: async (feeId) => { await service.from("match_fees").update({ status: "cancelled" }).eq("id", feeId) },
        now: () => Date.now(),
      },
      { feeId: row.id }
    )
    count++
  }
  return NextResponse.json({ swept: count })
}
```

- [ ] **Step 2: vercel.json 크론 등록**

`vercel.json`의 `crons` 배열에 추가(기존 크론 유지):

```json
{ "path": "/api/apps-in-toss/match-fee/sweep", "schedule": "0 * * * *" }
```

> Hobby 플랜은 크론 일1회 제한 — 스케줄이 거부되면 `"0 3 * * *"`(매일 1회)로 두고 lazy 정리에 의존.

- [ ] **Step 3: 타입체크 + Commit**

Run: `npx tsc --noEmit`
Expected: 에러 없음

```bash
git add app/api/apps-in-toss/match-fee/sweep/route.ts vercel.json
git commit -m "feat: 매칭이용료 만료 스윕 크론 백스톱"
```

---

### Task 10: 전체 검증

- [ ] **Step 1: 전체 테스트**

Run: `npx vitest run`
Expected: 신규 tossPay(5) + matchFeeFlow(9) + approveBid(갱신) 포함 전부 PASS

- [ ] **Step 2: 타입체크(루트 + 미니앱)**

Run: `npx tsc --noEmit && cd apps-in-toss && npx tsc -p tsconfig.app.json --noEmit`
Expected: 에러 없음

- [ ] **Step 3: 미니앱 빌드**

Run: `cd apps-in-toss && VITE_SUPABASE_URL=https://x VITE_SUPABASE_ANON_KEY=dummy npx vite build`
Expected: 성공

- [ ] **Step 4: 로컬 화면 검증(DEV 로그인)**

`cd apps-in-toss && npx vite --port 4791` 실행 후 브라우저에서: 기사 테스트로그인 → 내 입찰·매칭 → 매칭건 → MatchDetail에서 미결제 시 "결제하고 연락처 받기" CTA + 연락처·채팅 잠금 확인. (실결제는 mTLS·승인 후 샌드박스에서 `isTest:true`로 검증)

---

## 배포 전제(live)

- 018 프로덕션 적용(선행) + 019 프로덕션 적용.
- 앱 정보 심사 승인 → 토스페이 **가맹점 키** 청약 + mTLS 인증서 발급.
- env: `TOSS_PARTNER_API_BASE`(설정됨), `TOSS_MTLS_CERT`/`TOSS_MTLS_KEY`(login과 공용, 발급 후 설정).
- 토스 채널톡으로 "매칭 이용료 = 토스페이 vs 인앱결제(IAP)" 분류 사전확정. IAP로 판정 시 `tossPay.ts`만 IAP SDK로 교체(경계 격리됨).
- 결제 붙은 버전으로 앱인토스 콘솔 재검토 요청.
