# 앱인토스 미니앱 스캐폴딩 + 토스로그인 교환 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** (#2) 같은 레포에 앱인토스용 Vite+React SPA를 스캐폴딩하고, (#3-core) 토스로그인 authorizationCode→userKey 교환 순수 로직을 기존 Next 백엔드에 TDD로 추가한다.

**Architecture:** A′ 모노레포 — 미니앱 프론트는 `apps-in-toss/`(Vite SPA, 자체 package.json, `dist` 번들→콘솔 업로드), 백엔드는 기존 Next(Vercel) 재사용. 이 플랜은 (a) 프론트 스캐폴딩 + appLogin 화면, (b) 백엔드 토큰교환 순수함수(주입형 fetch, mTLS는 라우트 어댑터가 주입)까지. 라우트 오케스트레이션·세션 발급·mTLS 실배선·live 검증은 **후속 플랜**(credential/인증서/세션발급 방식 확정 후).

**Tech Stack:** Vite, React 18, TypeScript, @apps-in-toss/web-framework / 기존 Next.js, vitest.

---

## 파일 구조

- `lib/apps-in-toss/tossLogin.ts` (Create, 루트 Next) — `exchangeTossCode()`, `fetchTossUser()` 순수 함수(주입형 fetch)
- `lib/apps-in-toss/tossLogin.test.ts` (Create) — 단위 테스트(fetch 목)
- `apps-in-toss/` (Create, 서브 Vite 앱) — package.json, granite.config.ts, index.html, src/main.tsx, src/App.tsx
- `vitest.config.ts` (Modify, 루트) — `apps-in-toss/**` 를 test include에서 제외(서브앱 테스트 격리)

---

## Task 1: 토스 토큰교환 순수 로직 (TDD, 루트 Next)

**Files:**
- Create: `lib/apps-in-toss/tossLogin.ts`
- Test: `lib/apps-in-toss/tossLogin.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성** — `lib/apps-in-toss/tossLogin.test.ts`:
```ts
import { describe, it, expect } from "vitest"
import { exchangeTossCode, fetchTossUser } from "./tossLogin"

function okFetch(json: unknown) {
  return (async () => ({ ok: true, status: 200, json: async () => json })) as unknown as typeof fetch
}
function errFetch(status: number) {
  return (async () => ({ ok: false, status, json: async () => ({}) })) as unknown as typeof fetch
}
const cfg = { apiBase: "https://api.example.com" }

describe("exchangeTossCode", () => {
  it("authorizationCode를 토큰으로 교환한다", async () => {
    const f = okFetch({ accessToken: "at", refreshToken: "rt", expiresIn: 3600 })
    const r = await exchangeTossCode(f, cfg, "code123", "DEFAULT")
    expect(r).toEqual({ accessToken: "at", refreshToken: "rt", expiresIn: 3600 })
  })
  it("실패 응답이면 에러를 던진다", async () => {
    await expect(exchangeTossCode(errFetch(401), cfg, "code123", "DEFAULT"))
      .rejects.toThrow("토큰 발급 실패: 401")
  })
})

describe("fetchTossUser", () => {
  it("accessToken으로 사용자 정보를 조회한다", async () => {
    const f = okFetch({ userKey: "uk_1", name: "홍길동", phone: "010" })
    const r = await fetchTossUser(f, cfg, "at")
    expect(r).toEqual({ userKey: "uk_1", name: "홍길동", phone: "010" })
  })
  it("실패 응답이면 에러를 던진다", async () => {
    await expect(fetchTossUser(errFetch(403), cfg, "at"))
      .rejects.toThrow("사용자 조회 실패: 403")
  })
})
```

- [ ] **Step 2: 실행하여 실패 확인** — Run: `npm test`. Expected: FAIL(`./tossLogin` 없음)

- [ ] **Step 3: 구현** — `lib/apps-in-toss/tossLogin.ts`:
```ts
// 토스로그인 파트너 API 교환 순수 로직.
// 파트너 인증은 mTLS 클라이언트 인증서로 하며(문서 기준), 그 배선은
// 호출부(라우트)가 fetch 구현(dispatcher 포함)을 주입해 처리한다.
// 여기서는 주입된 fetchFn만 사용해 순수/테스트가능하게 유지한다.

export interface TossLoginConfig {
  apiBase: string
}
export interface TossTokenResponse {
  accessToken: string
  refreshToken: string
  expiresIn: number
}
export interface TossUser {
  userKey: string
  name?: string
  phone?: string
}

const GENERATE_TOKEN = "/api-partner/v1/apps-in-toss/user/oauth2/generate-token"
const LOGIN_ME = "/api-partner/v1/apps-in-toss/user/oauth2/login-me"

export async function exchangeTossCode(
  fetchFn: typeof fetch,
  cfg: TossLoginConfig,
  authorizationCode: string,
  referrer: string
): Promise<TossTokenResponse> {
  const res = await fetchFn(`${cfg.apiBase}${GENERATE_TOKEN}`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ authorizationCode, referrer }),
  })
  if (!res.ok) {
    throw new Error(`토큰 발급 실패: ${res.status}`)
  }
  const json = await res.json()
  return {
    accessToken: json.accessToken,
    refreshToken: json.refreshToken,
    expiresIn: json.expiresIn,
  }
}

export async function fetchTossUser(
  fetchFn: typeof fetch,
  cfg: TossLoginConfig,
  accessToken: string
): Promise<TossUser> {
  const res = await fetchFn(`${cfg.apiBase}${LOGIN_ME}`, {
    headers: { Authorization: `Bearer ${accessToken}` },
  })
  if (!res.ok) {
    throw new Error(`사용자 조회 실패: ${res.status}`)
  }
  const json = await res.json()
  return { userKey: json.userKey, name: json.name, phone: json.phone }
}
```

- [ ] **Step 4: 통과 확인** — Run: `npm test`. Expected: PASS(4 신규 + 기존 identity 4 = 8)
- [ ] **Step 5: 타입체크** — Run: `npx tsc --noEmit`. Expected: exit 0
- [ ] **Step 6: Commit**
```bash
git add lib/apps-in-toss/tossLogin.ts lib/apps-in-toss/tossLogin.test.ts
git commit -m "feat: 토스로그인 토큰교환 순수 로직(exchangeTossCode/fetchTossUser) + 테스트"
```

---

## Task 2: 루트 vitest가 서브앱 테스트 안 긁게 격리

**Files:** Modify: `vitest.config.ts`

- [ ] **Step 1: include에 서브앱 제외 추가** — `vitest.config.ts`를 다음으로 교체:
```ts
import { defineConfig } from "vitest/config"

export default defineConfig({
  test: {
    environment: "node",
    include: ["**/*.test.ts"],
    exclude: ["**/node_modules/**", "apps-in-toss/**"],
  },
})
```
- [ ] **Step 2: 확인** — Run: `npm test`. Expected: 기존 8 테스트만 실행, PASS
- [ ] **Step 3: Commit**
```bash
git add vitest.config.ts
git commit -m "chore: 루트 vitest에서 apps-in-toss 서브앱 제외"
```

---

## Task 3: 앱인토스 Vite SPA 스캐폴딩 (#2)

**Files:** Create: `apps-in-toss/` (Vite React 앱)

주의: 서브폴더는 **자체 package.json**을 가진 독립 npm 프로젝트다(루트 Next와 의존성 분리). 스캐폴딩 CLI가 파일을 생성하므로, 생성 후 우리 값으로 `granite.config.ts`와 `src/App.tsx`를 덮어쓴다.

- [ ] **Step 1: Vite React-TS 앱 생성**
```bash
cd /Users/park/Desktop/탁카
npm create vite@latest apps-in-toss -- --template react-ts
cd apps-in-toss
npm install
```
Expected: `apps-in-toss/`에 vite react-ts 템플릿 생성, 설치 완료

- [ ] **Step 2: 앱인토스 프레임워크 설치 + init**
```bash
cd /Users/park/Desktop/탁카/apps-in-toss
npm install @apps-in-toss/web-framework
npx ait init
```
Expected: `granite.config.ts` 생성(또는 갱신). ait init이 대화형이면 기본값 수용.

- [ ] **Step 3: granite.config.ts를 우리 값으로 작성** — `apps-in-toss/granite.config.ts`:
```ts
import { defineConfig } from "@apps-in-toss/web-framework/config"

export default defineConfig({
  appName: "takca",
  brand: {
    displayName: "탁카",
    primaryColor: "#F97316",
    icon: "", // 콘솔에서 업로드한 로고 URL로 교체
  },
  web: {
    host: "localhost",
    port: 5173,
    commands: {
      dev: "vite dev",
      build: "vite build",
    },
  },
  permissions: [],
  outdir: "dist",
})
```

- [ ] **Step 4: 로그인 화면 작성** — `apps-in-toss/src/App.tsx`:
```tsx
import { useState } from "react"
import { appLogin } from "@apps-in-toss/web-framework"

// 백엔드(기존 Next/Vercel) 로그인 엔드포인트. 배포 시 실제 도메인으로.
const BACKEND = import.meta.env.VITE_BACKEND_URL ?? "https://takca.vercel.app"

export default function App() {
  const [status, setStatus] = useState<string>("")

  async function handleLogin() {
    try {
      setStatus("토스 로그인 중…")
      const { authorizationCode, referrer } = await appLogin()
      const res = await fetch(`${BACKEND}/api/apps-in-toss/login`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ authorizationCode, referrer }),
      })
      setStatus(res.ok ? "로그인 성공" : `로그인 실패: ${res.status}`)
    } catch (e) {
      setStatus(`오류: ${e instanceof Error ? e.message : String(e)}`)
    }
  }

  return (
    <main style={{ minHeight: "100vh", display: "flex", flexDirection: "column",
      alignItems: "center", justifyContent: "center", gap: 24, fontFamily: "-apple-system, sans-serif" }}>
      <h1 style={{ fontSize: 28, fontWeight: 800 }}>
        탁<span style={{ color: "#F97316" }}>카</span>
      </h1>
      <p style={{ color: "#6B7280" }}>차량 탁송 직거래</p>
      <button onClick={handleLogin}
        style={{ background: "#F97316", color: "#fff", border: 0, borderRadius: 12,
          padding: "16px 28px", fontSize: 18, fontWeight: 700 }}>
        토스로 시작하기
      </button>
      <div style={{ color: "#9CA3AF", fontSize: 14 }}>{status}</div>
    </main>
  )
}
```

- [ ] **Step 5: 빌드 검증** — Run:
```bash
cd /Users/park/Desktop/탁카/apps-in-toss && npm run build
```
Expected: 에러 없이 `apps-in-toss/dist/` 생성. (`appLogin`은 토스 런타임에서만 실제 동작 — 빌드/타입 통과만 확인)

- [ ] **Step 6: 루트 .gitignore에 서브앱 산출물 제외 확인** — `apps-in-toss/node_modules`, `apps-in-toss/dist`가 무시되는지 확인. 안 되면 루트 `.gitignore`에 추가:
```
apps-in-toss/node_modules
apps-in-toss/dist
```

- [ ] **Step 7: Commit**
```bash
cd /Users/park/Desktop/탁카
git add apps-in-toss .gitignore
git commit -m "feat: 앱인토스 Vite SPA 스캐폴딩 + 토스로그인 화면(appLogin→백엔드)"
```

---

## 후속(이 플랜 범위 밖, 다음 플랜)

- `app/api/apps-in-toss/login/route.ts` — exchangeTossCode→fetchTossUser→resolveUserByTossKey→**Supabase 세션 발급**(발급 방식 확정 필요: admin.generateLink+verifyOtp 등) 오케스트레이션
- **mTLS 어댑터** — 파트너 API용 클라이언트 인증서로 fetch dispatcher 구성(undici Agent). 인증서 발급·Vercel 아웃바운드 검증(R6)
- API 베이스 호스트 확정(R7), client 스코프/암호화 필드 복호화
- 실 sandbox에서 appLogin→로그인 end-to-end 검증

## 자체 검토
- **커버리지:** #2 스캐폴딩(Task 3) + #3 교환 순수로직(Task 1) 담당. 라우트·세션·mTLS는 후속 명시.
- **자리표시자:** granite icon/BACKEND는 실제 기본값+교체 주석(런타임 설정, placeholder 아님). ait init 산출물은 CLI 생성이라 명령으로 처리.
- **타입 일관성:** `exchangeTossCode(fetchFn,cfg,code,referrer)`·`fetchTossUser(fetchFn,cfg,token)`·`TossLoginConfig{apiBase}` 구현/테스트 동일. userKey는 후속 라우트에서 `resolveUserByTossKey`로 연결.
